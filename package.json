{
  "name": "ha-floorplan",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "shx rm -rf dist/*",
    "start": "npm run clean && parcel src/index.html",
    "build-docs": "npm run clean && parcel build src/index.html --public-url ./",
    "build-demo": "npm run clean && parcel build src/index.html --public-url ./",
    "build-prod": "npm run clean && parcel build src/lib/floorplan/floorplan-exports.ts --out-file floorplan",
    "build-test": "npm run clean && parcel build src/lib/floorplan/floorplan-exports.ts --out-file floorplan --no-minify",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/debounce": "^1.2.0",
    "@types/js-yaml": "^3.12.5",
    "@types/node": "^14.14.10",
    "babel-core": "^6.26.3",
    "cross-env": "^7.0.2",
    "gh-pages": "^3.1.0",
    "parcel-bundler": "^1.12.4",
    "parcel-plugin-static-files-copy": "^2.5.0",
    "parcel-plugin-wrapper": "^0.2.3",
    "shx": "^0.3.3",
    "typescript": "^4.1.2"
  },
  "dependencies": {
    "debounce": "^1.2.0",
    "js-yaml": "^3.14.0"
  },
  "browserslist": [
    "last 1 Chrome version"
  ]
}
