name: Test on PR

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  checks: write

jobs:
  build:
    name: Build and Test
    uses: ./.github/workflows/common-build-test.yaml
    with:
      upload-artifacts: false